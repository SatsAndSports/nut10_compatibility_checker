# AGENTS.md

## Project Rules

- Keep the compatibility runner separate from `cdk/` and `nutshell/`.
- Do not modify upstream cloned repos unless explicitly required.
- Prefer the smallest useful change.

## Maintainer Notes

- Public results live in `README.md`.
- `README.md` is generated from `_README.md` and the tracked JSON reports in `reports/`.
- The runner lives in `compat-runner/`.
- Update this file if durable project rules or maintainer workflow change.
